<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/liste_livres_selector"
    android:padding="8dp" >

    <!-- Image  du livre-->
    <ImageView
        android:src="@drawable/ic_launcher"
        android:id="@+id/image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentLeft="true"
        android:layout_marginRight="8dp" />

    <!-- Titre du livre -->
    <TextView
        android:id="@+id/titre"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/image"
        android:layout_toRightOf="@+id/image"
        android:textSize="@dimen/titre"
        android:textStyle="bold" />

    <!-- Auteur du livre -->
    <TextView
        android:id="@+id/auteur"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/titre"
        android:layout_marginTop="1dip"
        android:layout_toRightOf="@+id/image"
        android:textSize="@dimen/auteur" />

    <!-- Categorie -->
    <TextView
        android:id="@+id/categorie"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/auteur"
        android:layout_marginTop="5dp"
        android:layout_toRightOf="@+id/image"
        android:textColor="@color/categorie"
        android:textSize="@dimen/categorie" />

    <!-- Annee de realisation -->
    <TextView
        android:id="@+id/annee"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:textColor="@color/annee"
        android:textSize="@dimen/annee" />
    <ImageView
        android:layout_width="@dimen/arrowsize"
        android:layout_height="@dimen/arrowsize"
        android:src="@drawable/arrow"

        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"/>

</RelativeLayout>